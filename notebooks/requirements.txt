# Requirements for running notebooks under notebooks/.
# This does NOT include aiperf itself — the scenario notebook shells out to
# the scenario bash scripts, and the aiperf_uc*.ipynb use-case notebooks
# invoke the `aiperf` CLI directly; either way aiperf comes from whatever
# environment/image it's installed in (see each notebook's Requirements
# section for how to point at it).
jupyter>=1.0
pandas>=2.0
matplotlib>=3.8

# Needed by aiperf's tokenizer loading for some models with custom
# `trust_remote_code` config/model classes (e.g. embedding models with a
# custom auto_map, such as nvidia/llama-nemotron-embed-1b-v2) — without it,
# tokenizer loading can fail with an AttributeError on the fallback base
# config class instead of a clear "torch not found" error. See the
# UC7 notebook's tokenizer troubleshooting note.
torch>=2.0

# Model-quality evaluation notebook (model_eval_deepeval.ipynb):
# deepeval provides the LLM-as-judge metrics; openai is the client used to
# query the model under test (any OpenAI-compatible endpoint).
deepeval>=1.0
openai>=1.0
